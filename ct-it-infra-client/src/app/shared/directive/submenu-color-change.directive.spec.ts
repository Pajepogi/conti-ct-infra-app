import { SubmenuColorChangeDirective } from './submenu-color-change.directive';

describe('SubmenuColorChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new SubmenuColorChangeDirective();
    expect(directive).toBeTruthy();
  });
});
